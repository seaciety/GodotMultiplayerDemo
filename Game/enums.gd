extends Node

# TODO: consolidate with proto files to avoid duplicates
enum movement_input {NONE, MOVE_RIGHT, MOVE_LEFT}
enum states {IDLE, MOVE, ATTACK, DEAD, JUMPING, FALLING, LANDING}
enum directions {RIGHT, LEFT}
